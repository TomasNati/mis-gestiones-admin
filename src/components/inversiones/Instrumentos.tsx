import React from "react";
import { Box, Typography } from "@mui/material";

export const Instrumentos: React.FC = () => {
  return (
    <Box>
      <Typography variant="h5">Instrumentos (dummy)</Typography>
      <Typography variant="body1">This is a placeholder component for Instrumentos.</Typography>
    </Box>
  );
};
